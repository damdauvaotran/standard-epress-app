export enum ENodeEnv {
  dev = 'development',
  prod = 'production',
}

export enum EGender {
  male = 'MALE',
  female = 'FEMALE',
  other = 'OTHER',
}

export enum EApiMethod {
  get = 'GET',
  post = 'POST',
  put = 'PUT',
  patch = 'PATCH',
  delete = 'DELETE',
}

export enum EApiSampleMode {
  static = 'STATIC',
  dynamic = 'DYNAMIC',
}
